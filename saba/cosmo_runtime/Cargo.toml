[package]
name = "cosmo_runtime"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"

[dependencies]
cosmo_engine = { path = "../cosmo_engine" }
cosmo_script = { path = "../cosmo_script" }
encoding_rs = "0.8"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
