[package]
name = "cosmo_core"
version = "0.1.0"
edition = "2021"

[dependencies]
cosmo_core_legacy = { path = "../cosmo_core_legacy" }
cosmo_runtime = { path = "../cosmo_runtime" }

[dev-dependencies]
serde_json = "1"
